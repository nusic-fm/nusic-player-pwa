export interface OpenseaNFT {
  animationUrl: string;
  imagePreviewUrl: string;
  name: string;
  description?: string;
  address: string;
  tokenId: string;
  collectionName: string;
  openseaName: string;
  chain?: string;
  artist?: string;
}
